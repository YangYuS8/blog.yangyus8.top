# ===================== Waline & MariaDB Secrets =====================
# 复制为 .env 后修改真实值，不要提交 .env

# MariaDB 基础变量（供 mariadb 官方镜像初始化）
MYSQL_ROOT_PASSWORD=replace_root_password
MYSQL_DATABASE=waline
MYSQL_USER=waline
MYSQL_PASSWORD=replace_user_password
# 为兼容 Waline 1.x 服务端的变量命名 (Waline 读取 MYSQL_DB)，docker-compose 已将 MYSQL_DB 映射为 MYSQL_DATABASE。
# 如需显式写也可以取消注释：
# MYSQL_DB=waline
# MariaDB 数据目录 (容器内 /var/lib/mysql)
WALINE_DB_DIR=./data/waline-db

# Waline 管理账号（首次登录后台）
ADMIN_USER=admin
ADMIN_PASSWORD=replace_admin_password
# Waline 数据目录 (容器内 /data)
WALINE_DATA_DIR=./data/waline

# 站点公开访问地址（用于注入前端 & 邮件等绝对链接，如果需要）
SITE_URL=https://blog.yangyus8.top
# Public URL for Waline when front-end needs it (optional override)
WALINE_PUBLIC_URL=https://blog.yangyus8.top/comment/

# 镜像名称（用于 docker-compose 拉取）
BLOG_IMAGE=ghcr.io/OWNER/yangyus8-blog:latest

# 可选邮件通知配置（示例）
# SMTP_HOST=smtp.yangyus8.top
# SMTP_PORT=465
# SMTP_USER=mailer@yangyus8.top
# SMTP_PASS=replace_smtp_password
# SMTP_SECURE=true

# 时区
TZ=Asia/Shanghai

# ===================== MinIO (可选) =====================
# MinIO 控制台建议由上层反代并加 BasicAuth/限制访问来源
MINIO_ROOT_USER=replace_minio_user
MINIO_ROOT_PASSWORD=replace_minio_password
MINIO_BUCKET=blog
# 供前端引用的公开前缀（建议配置到反向代理域名，如 https://cdn.yangyus8.top）
MINIO_PUBLIC_PREFIX=https://cdn.yangyus8.top
# MinIO 数据目录 (容器内 /data)
MINIO_DATA_DIR=./data/minio

# ===================== Twikoo (Self-host) =====================
# Twikoo 公网访问前缀（需与 _config.fluid.yml 中 twikoo.envId 保持一致）
TWIKOO_PUBLIC_URL=https://blog.yangyus8.top/twikoo/
# Twikoo MongoDB 连接串（副本集初始化后可加 ?replicaSet=rs0）
TWIKOO_MONGO_URL=mongodb://twikoo-db:27017/twikoo
# 允许的跨域来源（多个用逗号）
TWIKOO_CORS_ALLOW_ORIGIN=https://blog.yangyus8.top
# Twikoo 数据目录（若后续启用本地文件特性，可自定义挂载；当前镜像主要使用 Mongo 存储）
TWIKOO_DB_DIR=./data/twikoo-db
